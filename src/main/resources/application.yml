server:
  port: 7081
  servlet:
    context-path: /demo

spring:
  rabbitmq:
    addresses: localhost
    port: 5672
    username: root
    password: root

  application:
    name: websocket


  freemarker:
    request-context-attribute: request

